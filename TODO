superadmin can see
show which category made how many tokens
how many tokens redeemed


categories can issue tokens to organisers by loggin in and sending mail of url->QR which will be scanned by cafeteria and that qr is now invalid

each category has diff login


Requirements
every category has their own login system and they can issue tokens which will be tracked in tokens model 
